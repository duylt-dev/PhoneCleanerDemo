// compose-stability.conf   (// comments only — a # breaks the parse)
// Types :domain and :core:common export are inferred unstable because those modules are
// compiled without the Compose plugin (LLM.md §2). Declared here rather than by adding a
// Compose dependency to :domain. Every entry must be genuinely immutable after construction —
// ComposeStabilityReportTest is the gate that keeps this honest (LLM.md §8).
kotlin.collections.List
kotlin.collections.Map
kotlin.collections.Set
kotlin.time.Instant
kotlinx.datetime.LocalDate
kotlinx.collections.immutable.ImmutableList
kotlinx.collections.immutable.ImmutableSet
kotlinx.collections.immutable.ImmutableMap
kotlinx.collections.immutable.PersistentList
com.pion.phonecleaner.core.common.error.AppError
com.pion.phonecleaner.core.common.result.AppResult
com.pion.phonecleaner.core.common.format.FormattedSize
com.pion.phonecleaner.domain.model.*
com.pion.phonecleaner.domain.catalog.*
