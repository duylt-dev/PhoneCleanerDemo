<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneCleaner">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhoneCleaner"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The lock surface. NOT exported: the competitor's equivalent is, and takes an arbitrary
            `pkg` extra from any app on the device (AndroidManifest.xml:246). singleTask so a second
            prompt cannot stack on the first; excludeFromRecents so the recents thumbnail does not
            name the app the lock is protecting. See LockScreenActivity's KDoc.
        -->
        <activity
            android:name=".LockScreenActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.PhoneCleaner"
            android:windowSoftInputMode="adjustResize" />

        <!--
            WorkManager's default on-demand initializer is removed so Koin's worker factory is the one
            that builds a Worker (LLM.md §6.3, `workerOf`). Left in place, androidx.startup initialises
            WorkManager with the DEFAULT factory before startKoin runs, and the first worker with a
            constructor argument fails to instantiate — at 3am, in a job nobody is watching.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
